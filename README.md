# dupCheck

```bash
dupCheck check -h                 
checks apps for potential duplicates

Usage:
  check check [flags]

Flags:
  -a, --application string    app to filter
  -e, --environment string   environment to filter
  -h, --help                 help for check
```
